interface:
  display_name: "HTML Artifact Generator"
  short_description: "Create polished standalone HTML artifacts"
  default_prompt: "Use $html-artifact-generator to turn this Markdown or discussion into a polished standalone HTML artifact."
